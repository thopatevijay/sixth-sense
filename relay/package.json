{
  "name": "sixth-sense-relay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "SIXTH SENSE data spine — one relay, three modes (LIVE / REPLAY / MOCK), one output contract.",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
